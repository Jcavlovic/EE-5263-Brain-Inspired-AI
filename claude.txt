Target Dataset

    Labeled Faces in the Wild

Target Framework (e.g., snnTorch, pure PyTorch, etc.)

    PyTorch

Type of Neural Network

    Rate-based Neural Networks

Network Architecture

    Number of hidden layers [1, 2, 3, 4]

    Neurons per layer [64, 128, 256, 512]

    Weight initialization techniques [Normal]

    Sparsity [10%, 20%, 50%, 80%]

Learning Rule/Mechanisms

    Learning rate [0.1, 0.001, 0.0001]

    Number of Epochs [25, 100, 200]

    Batch Size [32, 128]

    Activation Function [Sigmoid]

Pre-Processing of Input Data

    Grayscale Images (For LFW dataset only)

Local Plasticity

    Supervised Learning

Network Classification Task

    Gender Classification

Create a Rate based neural network using pytorch with the above parameters using the Labeled Faces in the wild dataset that is located in the data folder. Use GPU if available to train and test.
Output the results of the model to graphs using matplotlib for each paramater. Apply greyscale to the dataset before training and testing, and when training and testing determine if the image is male or female only.